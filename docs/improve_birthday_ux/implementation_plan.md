# 実装計画: 生年月日のUX改善

## 概要
ユーザーからのリクエストにより、生年月日の入力欄の初期値を30年前に設定し、入力しやすくする。

## 手順
1. `app/islands/Dashboard.tsx` で `inputValue` の初期値を `''` から `現在日付 - 30年` の日付文字列に変更する。
2. 変更をコミットし、プッシュする。
3. PRを作成する。

## 詳細
- `Date` オブジェクトを使用して30年前の日付を計算。
- `YYYY-MM-DD` 形式でフォーマット。
- `useState` の初期値として設定。
